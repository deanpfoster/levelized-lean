import CslibHeaders.Basic
import CslibHeaders.Defs.Languages.CCS.Semantics

/-! # CCS Semantics -- Transition Relation

  Vocabulary:
    Tr          -- transition relation for CCS (labelled transition system)
    Terminated  -- a process is terminated (composition of nils)

  This module defines the operational semantics of CCS.
  No theorems -- see BehaviouralTheory for bisimilarity results.
-/
